import { LectureData } from '@/data/lectures/types'
import { Slide01IfElse } from './Slide01IfElse'

export const lab3IfElseData: LectureData = {
  id: 'lab-3-ifelse',
  title: 'Lab 3 · If / else',
  slides: [
    { title: 'If / else', content: <Slide01IfElse /> },
  ],
}
