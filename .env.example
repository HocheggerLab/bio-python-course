# Bearer token for poll admin actions (open/close/reset) and reading results.
# Generate with:  openssl rand -hex 32
POLL_ADMIN_TOKEN=

# Neon Postgres connection string. When set, the poll uses the SQL store
# instead of the development-only in-memory one.
DATABASE_URL=
